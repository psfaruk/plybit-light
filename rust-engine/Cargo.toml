[package]
name    = "playbit-engine"
version = "0.1.0"
edition = "2021"

[lib]
name    = "playbit_engine"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto       = true
